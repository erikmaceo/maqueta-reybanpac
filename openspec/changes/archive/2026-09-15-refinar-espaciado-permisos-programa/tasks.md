## 1. Ajustes de la pantalla

- [x] 1.1 Eliminar del template el título y subtítulo internos, conservando el botón superior `Volver` y su navegación.
- [x] 1.2 Cambiar el padding de `.permission-card` de `5px` a `10px` sin modificar el ancho completo ni la paginación existente.
- [x] 1.3 Mover el grupo inferior `Volver`/`Guardar` fuera de `.permission-card` y aplicar `margin-top: 10px`, alineación derecha y `gap: 2px`.

## 2. Verificación

- [x] 2.1 Ejecutar `npm run build` en `front-angular` y corregir errores de compilación o templates.
- [x] 2.2 Verificar que el header global conserve el título y descripción, que la tarjeta tenga el nuevo espaciado y que las acciones queden debajo.
- [x] 2.3 Confirmar que `Volver`, `Guardar`, paginación, carga, errores y navegación existente mantengan su comportamiento.
