## 1. Alineación en pantalla de Aplicación

- [x] 1.1 Agregar bloque `styles` en `AplicacionFormComponent` con `.form-actions { display: flex; justify-content: flex-end; gap: 5px; margin-top: 24px; }`.

## 2. Alineación en pantalla de Módulo

- [x] 2.1 Agregar bloque `styles` en `ModuloFormComponent` con `.form-actions { display: flex; justify-content: flex-end; gap: 5px; margin-top: 24px; }`.

## 3. Alineación en pantalla de Programa

- [x] 3.1 Agregar bloque `styles` en `ProgramaFormComponent` con `.form-actions { display: flex; justify-content: flex-end; gap: 5px; margin-top: 24px; }`.

## 4. Verificación

- [x] 4.1 Ejecutar `npm.cmd run build` en `front-angular` y confirmar compilación sin errores.
- [ ] 4.2 Verificar visualmente en las rutas nuevo/editar de las tres entidades que el bloque de acciones está a la derecha con separación de 5px y sin cambios en el comportamiento de los botones.